% Test time mechanisms

% Get current time
get_time(T?) :- true | now(T).

% Wait for 500ms - wait is a guard, must have |
wait_test(done) :- wait(500) | true.

% Wait until specific time (pass timestamp as arg)
until_test(T, done) :- wait_until(T?) | true.
