% GLP System Predicate: = (Unification)
%
% Binds the second argument (writer) to the first argument's value.
%
% Usage:
%   3 = X     - Binds X to 3
%   Y? = X    - Binds X to Y's value
%
% Semantics:
%   X? = Y succeeds if Y is a writer.
%   X? can be a reader or a ground term (non-var).

X? = Y.
